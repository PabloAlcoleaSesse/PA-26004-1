/** @type {import('@react-router/dev/config').Config} */
export default {
  // Keep server rendering enabled so route loaders can evolve into a secure
  // backend-for-frontend boundary around the Go API.
  ssr: true,
};
